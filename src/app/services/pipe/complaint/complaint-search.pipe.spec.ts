import { ComplaintSearchPipe } from './complaint-search.pipe';

describe('ComplaintSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplaintSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
