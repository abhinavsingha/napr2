import { NotificationSearchPipe } from './notification-search.pipe';

describe('NotificationSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
